============
Installation
============

At the command line::

    $ easy_install django-cookie-monster

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-cookie-monster
    $ pip install django-cookie-monster
